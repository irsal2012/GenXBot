{
  "name": "genxbot",
  "version": "0.1.0",
  "description": "Global CLI for onboarding and daemonizing GenXBot",
  "license": "MIT",
  "bin": {
    "genxbot": "bin/genxbot.js"
  },
  "type": "commonjs",
  "scripts": {
    "release:check": "node ./scripts/release-check.cjs",
    "release:validate": "npm run release:check && npm pack --dry-run && npm publish --dry-run --access public"
  },
  "files": [
    "bin",
    "scripts",
    "CHANGELOG.md"
  ],
  "engines": {
    "node": ">=18"
  }
}